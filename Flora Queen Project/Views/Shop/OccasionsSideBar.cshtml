@model IEnumerable<Flora_Queen_Project.Models.Occasion>

<ul>
    @foreach (var item in Model)
    {
        <li>
            <a href="#">@Html.DisplayFor(modelItem => item.Name)
                <span>
                    @Html.DisplayFor(modelItem => item.Products.Count)
                </span>
            </a>
        </li>
    }
</ul>
