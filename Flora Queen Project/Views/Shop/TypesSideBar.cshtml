@model IEnumerable<Flora_Queen_Project.Models.Type>

<ul>
    @foreach (var item in Model)
    {
        <li>
            <a class="type-click" data-id="@item.Id">
                @Html.DisplayFor(modelItem => item.Name)
                <span>
                    @Html.DisplayFor(modelItem => item.Products.Count)
                </span>
            </a>
        </li>
    }
</ul>
